# 비올리즈 스레드 발행 대기열 (Threads Auto-Post Queue)
# =====================================================================
# 규칙:
#  - approved: true 이고, scheduled_at(예약시간)이 지난 글만 자동 발행됩니다.
#  - 노션에서 '승인'하면 자동으로 여기에 추가됩니다.
#  - 한 번 실행에 1개만 발행됩니다(MAX_POSTS_PER_RUN).
# 시간대: scheduled_at 은 +09:00(KST).
# =====================================================================

posts: []
